<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
</head>
<body>
    <h1>Add Link</h1>
    <a href="/">Visualizar Links</a>

    <hr>

    <form action="/" method="POST">
        <input type="text" name="title" class="field" placeholder="Title" value="<%=body.title%>">
        <input type="text" name="description" class="field" placeholder="Description" value="<%=body.description%>">
        <input type="text" name="url" class="field" placeholder="URL" value="<%=body.url%>">
        <button>Salvar</button>
    </form>

    <% if(err){ %>
        <h3><%=err.message%></h3>
    <% } %>

</body>
</html>